import 'package:flutter/material.dart';

class PayAtCounterScreen extends StatelessWidget {
  const PayAtCounterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
